# Template JAVA com Grandle e Docker 

Para rodar o projeto é necessário ter o docker instalado, para iniciar o projeto utilize o seguinte comando: 
```bash
docker-compose up --build --force-recreate --no-deps
```
